{
  "$schema": "./circuits.schema.json",
  "groupOrder": [
    "Relays · Basics",
    "Relays · Gates",
    "Relays · Memory",
    "Relays · Arithmetic",
    "General",
    "CMOS · Basics",
    "CMOS · Gates",
    "CMOS · Buses",
    "CMOS · Memory",
    "CMOS · Sequential",
    "CMOS · Machines",
    "CMOS · Arithmetic",
    "NMOS · Basics",
    "NMOS · Gates",
    "NMOS · Memory",
    "NMOS · Arithmetic",
    "NMOS · Arrays"
  ],
  "circuits": [
    {
      "id": "relay101",
      "group": "Relays · Basics",
      "name": "Meet the Relay",
      "desc": "One relay: energize the coil and the armature snaps over, closing the normally-open contact and opening the normally-closed one. Everything else here is built from just this."
    },
    {
      "id": "buzzer",
      "group": "Relays · Basics",
      "name": "Buzzer",
      "desc": "The relay interrupts its own coil through its NC contact, so it chatters forever — the classic doorbell buzzer. Hold PRESS and crank the speed up."
    },
    {
      "id": "osc",
      "group": "Relays · Basics",
      "name": "Ring Oscillator",
      "desc": "Three NOT relays in a loop. An odd number of inversions can never settle, so the pulse chases itself around the ring at a speed set by the relay delay."
    },
    {
      "id": "not",
      "group": "Relays · Gates",
      "name": "NOT",
      "desc": "Inversion is free with relays: the normally-closed contact conducts exactly when the coil is off."
    },
    {
      "id": "and",
      "group": "Relays · Gates",
      "name": "AND",
      "desc": "Two normally-open contacts in series — current only gets through when both relays pull in."
    },
    {
      "id": "or",
      "group": "Relays · Gates",
      "name": "OR",
      "desc": "Two normally-open contacts in parallel — either relay can complete the path."
    },
    {
      "id": "xor",
      "group": "Relays · Gates",
      "name": "XOR",
      "desc": "Two changeover paths criss-cross like a hallway light with two wall switches: the lamp is on when the switches disagree."
    },
    {
      "id": "nand",
      "group": "Relays · Gates",
      "name": "NAND",
      "desc": "Parallel normally-closed contacts: the light stays on unless both relays pull in. NAND alone is enough to build everything else."
    },
    {
      "id": "nor",
      "group": "Relays · Gates",
      "name": "NOR",
      "desc": "Series normally-closed contacts: on only when both relays are at rest."
    },
    {
      "id": "dec24",
      "group": "Relays · Gates",
      "name": "2-to-4 Decoder",
      "desc": "A contact tree: relay A splits the current two ways, relay B splits each again. Two inputs select exactly one of four lamps — this is how addresses select memory rows."
    },
    {
      "id": "t101",
      "group": "General",
      "name": "Meet the Transistor",
      "desc": "A MOSFET is a relay with no moving parts: the gate is isolated from the channel exactly like a coil is isolated from its contacts. N-channel conducts when the gate is high, P-channel when it is low — the two throws of a relay, split into two devices. Each output needs a resistor to hold it down, because a transistor that is off does not pull anything low; it just lets go."
    },
    {
      "id": "cmosinv",
      "group": "CMOS · Basics",
      "name": "CMOS Inverter",
      "desc": "Two transistors, and the whole of CMOS in miniature: the P-channel pulls the output up, the N-channel pulls it down, and the input turns exactly one of them on. Slow the clock right down and watch the handover — for a moment both are on (a red X, a dead short through the pair) or both are off (a dashed Z, the output floating on its own charge). Real chips fight both."
    },
    {
      "id": "cmosnand",
      "group": "CMOS · Gates",
      "name": "CMOS NAND",
      "desc": "Four transistors. The pull-down network is the logic — two N-channels in series, so both inputs must be high to reach ground — and the pull-up is its exact complement, two P-channels in parallel. Every static CMOS gate is built this way, and the pull-up is always the dual of the pull-down."
    },
    {
      "id": "cmosnor",
      "group": "CMOS · Gates",
      "name": "CMOS NOR",
      "desc": "The mirror image of NAND: series P-channels above, parallel N-channels below. Swap series for parallel in both networks and the gate inverts — which is why NAND and NOR come in pairs and why either one alone can build a computer."
    },
    {
      "id": "nmosinv",
      "group": "NMOS · Basics",
      "name": "NMOS Inverter",
      "desc": "How it was done before CMOS, and how the 4004 itself was built: one transistor and a resistor load. It works — but the load only ever drives weakly, so whenever the output is low there is a permanent path from the rail to ground. That static current is why early chips ran hot and why the second transistor was worth adding."
    },
    {
      "id": "diode",
      "group": "General",
      "name": "Diode Logic",
      "desc": "The one directional device here: a diode passes a 1 forward and a 0 backward, and blocks the other way. Point them one way with a pull-down and you get OR; turn them around under a pull-up and you get AND. No transistors at all — but also no gain, so the output is weaker than the input and these cannot be chained. That is exactly why amplifying devices had to be invented."
    },
    {
      "id": "cmosand",
      "group": "CMOS · Gates",
      "name": "CMOS AND",
      "desc": "There is no such thing as a CMOS AND gate — only a NAND with an inverter bolted on, which is what you are looking at. Six transistors where the NAND alone needs four, and one more gate delay. Compare the relay AND: two contacts in series, and inversion is the thing that costs extra there. Each technology makes a different gate cheap, and that shapes how logic gets written for it."
    },
    {
      "id": "cmosor",
      "group": "CMOS · Gates",
      "name": "CMOS OR",
      "desc": "A NOR plus an inverter, for the same reason AND is a NAND plus an inverter: the complementary pull-up network of a static CMOS gate always inverts. Nothing in CMOS produces a non-inverted output for free."
    },
    {
      "id": "cmosxor",
      "group": "CMOS · Gates",
      "name": "CMOS XOR",
      "desc": "Four NAND gates — sixteen transistors for a function a single relay changeover contact gives you for nothing (see the relay XOR). XOR is the gate where relays genuinely beat transistors on device count, and it is why the relay adder is so much smaller than you would guess: its sum output is pure changeover staircase."
    },
    {
      "id": "cmosdec",
      "group": "CMOS · Gates",
      "name": "CMOS 2-to-4 Decoder",
      "desc": "The same one-hot decode as the relay contact tree and the NMOS version, in complementary CMOS. This is the exact circuit that sits in front of the ROM array and the register file, choosing which row an address refers to — addressing is the operation all memory is built on."
    },
    {
      "id": "cmoslatch",
      "group": "CMOS · Memory",
      "name": "CMOS D Latch",
      "desc": "Two inverters and two transmission gates on complementary enables, so exactly one path is ever open: EN high and Q follows D, EN low and the inverter pair closes a loop that holds. NEN must be the complement of EN — drive them the same and you will see the cell contend. Compare the NMOS latch, which cannot use pass gates at all and needs cross-coupled NORs instead."
    },
    {
      "id": "cmosfa",
      "group": "CMOS · Arithmetic",
      "name": "CMOS Full Adder",
      "desc": "Sum is A XOR B XOR Cin, carry is the majority vote — the same logic as the relay full adder, built from CMOS gate modules. The relay version does it in three relays and eleven contacts; this takes 50 transistors, almost all of them spent on the two XORs."
    },
    {
      "id": "tgate",
      "group": "CMOS · Gates",
      "name": "Transmission Gate",
      "desc": "An N-channel and a P-channel wired in parallel, driven by complementary gates: together they pass a full 0 and a full 1 in either direction, which neither can do alone. Two of them make a 2-to-1 multiplexer — the building block relays get for free with a changeover contact, and the reason a relay contact is worth two transistors."
    },
    {
      "id": "tristate",
      "group": "CMOS · Buses",
      "name": "Tri-State Bus",
      "desc": "Two drivers, one shared wire, and nothing to arbitrate. Enable one and the bus follows it. Enable neither and the bus floats — dashed Z, holding its last value on stray capacitance until it leaks away. Enable both with different data and they fight: a red X, a rail-to-rail short. This is the thing relays cannot do, and it is what lets a CPU have buses instead of a multiplexer tree for every destination.",
      "hints": {
        "D": "what each driver would put on the bus (bit 0 = driver 1)",
        "EN": "which drivers are switched on (bit 0 = driver 1)",
        "BUS": "the shared wire they all drive"
      },
      "table": {
        "title": "What the bus does",
        "rows": [
          {
            "code": "1",
            "name": "One driver enabled",
            "note": "the bus follows it"
          },
          {
            "code": "2",
            "name": "Both, agreeing",
            "note": "no conflict — same value"
          },
          {
            "code": "Z",
            "name": "Neither enabled",
            "note": "floating, holds its last value"
          },
          {
            "code": "X",
            "name": "Both, disagreeing",
            "note": "contention — a real short"
          }
        ]
      }
    },
    {
      "id": "tech3",
      "group": "General",
      "name": "Three Technologies",
      "desc": "One NAND gate, built three ways from the same two inputs: two relays, two transistors and a resistor, then four transistors. Same truth table, same lamps, wildly different machines — and the CMOS output lands while the armatures are still travelling."
    },
    {
      "id": "srlatch",
      "group": "Relays · Memory",
      "name": "SR Latch",
      "desc": "Tap SET and the relay feeds its own coil through its own contact — it remembers. RESET breaks the hold path. One bit of memory."
    },
    {
      "id": "dlatch",
      "group": "Relays · Memory",
      "name": "D Latch",
      "desc": "While EN is on, Q follows D through one path; when EN drops, a second path lets Q hold itself. Flip D with EN off — nothing happens until you open the gate."
    },
    {
      "id": "reg4",
      "group": "Relays · Memory",
      "name": "4-bit Register",
      "desc": "Four D latches sharing one LOAD button through an 8-pole relay. Set a number on the D switches, tap LOAD, and the register keeps it."
    },
    {
      "id": "regfile",
      "group": "CMOS · Memory",
      "name": "16×4 Register File",
      "desc": "The 4004’s index registers: sixteen 4-bit words, written through one address port and read through another, so the machine can read one register while writing a different one. Every row drives the same four bit lines through tri-state gates — one shared bus, sixteen possible drivers. Set WA and D, raise WE to store, then read any address back. Drop WE before changing the address: these are level-sensitive latches, so moving the address with WE still high walks the word into the next register, exactly as the real part does.",
      "hints": {
        "WA": "write address — which of the 16 registers to store into",
        "D": "data to write",
        "WE": "write enable — raise to store, then drop before changing WA",
        "RA": "read address — independent of WA, so you can read while writing",
        "Q": "contents of register RA (always live)"
      },
      "table": {
        "title": "Writing a register",
        "rows": [
          {
            "code": "1.",
            "name": "Set WA and D",
            "note": "with WE low"
          },
          {
            "code": "2.",
            "name": "Raise WE",
            "note": "stored while high"
          },
          {
            "code": "3.",
            "name": "Drop WE",
            "note": "before touching WA again"
          },
          {
            "code": "4.",
            "name": "Set RA",
            "note": "Q follows it immediately"
          }
        ]
      },
      "state": {
        "title": "Register contents",
        "columns": 4,
        "key": "amber = the register RA is reading · blue = where WA would write"
      }
    },
    {
      "id": "cmospc4",
      "group": "CMOS · Sequential",
      "name": "4-bit Counter",
      "desc": "The first circuit here with a clock, and the first that has to remember where it was. Press RUN, then use the clock controls to step it by hand or let it free-run. Each stage is a master-slave flip-flop: two latches on opposite clock phases, so a new value can never reach the output in the same phase it arrived. That is what makes the feedback safe — built from plain latches the incremented value would race straight back through the adder and the count would jump several times per pulse. RST is synchronous, so releasing it costs one edge before counting starts, exactly as a real synchronous reset does.",
      "hints": {
        "CLK": "the clock — use the controls above, or flip it by hand",
        "RUN": "enable counting; low holds the current value",
        "RST": "clear to zero (synchronous — costs one edge)",
        "Q": "the count",
        "Cout": "carry out of the top bit — high on the tick that rolls over"
      }
    },
    {
      "id": "cmospc12",
      "group": "CMOS · Sequential",
      "name": "12-bit Program Counter",
      "desc": "The 4004's program counter: twelve bits, addressing 4 KB of program space. Same circuit as the 4-bit counter, three times as wide — which is the point of building it from a module. This is the piece that makes a machine able to run a program rather than compute one answer: something has to say which instruction is next, and advance on its own.",
      "hints": {
        "CLK": "the clock — use the controls above, or flip it by hand",
        "RUN": "enable counting; low holds the current address",
        "RST": "clear to zero (synchronous — costs one edge)",
        "Q": "the program address",
        "Cout": "carry out of the top bit — wraps at 4096"
      }
    },
    {
      "id": "fetch",
      "group": "CMOS · Machines",
      "name": "Fetch Machine",
      "desc": "The first circuit here that executes rather than computes. A program counter addresses a ROM, the byte at that address lands in an instruction register on the next clock edge, and a decoder lights the line for whichever instruction that is. Give it a clock and it runs on its own — everything before this needed you to flip a switch. Nothing acts on the decoded instruction yet: that is control sequencing, the next piece. So this is a machine that reads a program and understands it without doing anything about it, which is exactly the first milestone worth reaching. Two-byte instructions light TWO but are not yet honoured — the machine reads the operand byte as if it were an instruction, and watching that go wrong is the argument for building the sequencer next.",
      "hints": {
        "CLK": "the clock — run it, or step one edge at a time",
        "RUN": "let the program counter advance",
        "RST": "back to address 0 (synchronous — costs one edge)",
        "PC": "program counter: which byte is being fetched",
        "IR": "instruction register: the byte fetched last edge",
        "TWO": "this instruction takes an address byte after it"
      },
      "state": {
        "title": "Decoded instruction",
        "columns": 4,
        "key": "amber = the line the decoder is driving high right now"
      }
    },
    {
      "id": "sequenced",
      "group": "CMOS · Machines",
      "name": "Sequenced Machine",
      "desc": "The fetch machine with a control unit, and the difference is that this one separates the phases of an instruction. A ring counter generates FETCH, DECODE and EXEC, and the control unit ANDs each phase with the decoded instruction lines — that AND is the whole idea of a hardwired control unit: “we are in EXEC” AND “this is a JUN” is exactly the signal that would load the program counter. Step it one edge at a time and watch the ring rotate, the instruction hold still across all three phases, and the control lines fire only in the phase that owns them. The instruction register is the piece that makes this work: it loads only during FETCH, holding its value through DECODE and EXEC. Without that gate it would follow the ROM on every edge — and since the PC has already advanced by then, the control unit would be acting on one instruction while the register showed the next. What is still missing is the datapath. The control lines are generated and visible but nothing is wired to them yet: no accumulator, no register file, and the PC advances by counting rather than being loaded, so the loop you see is the 3-bit counter rolling over rather than the jump taking effect. Wiring those up is the next piece.",
      "hints": {
        "CLK": "the clock — one edge per phase, so three edges per instruction",
        "RST": "back to FETCH at address 0 (synchronous — costs one edge)",
        "P": "the phase ring: FETCH, DECODE, EXEC",
        "PC": "program counter",
        "IR": "instruction register",
        "JMP": "the control unit is loading the PC — a jump is happening"
      },
      "state": {
        "title": "Phases and control lines",
        "columns": 4,
        "key": "amber = the active phase · blue = a control line firing"
      }
    },
    {
      "id": "addmachine",
      "group": "CMOS · Machines",
      "name": "Adding Machine",
      "desc": "The 4004's real ADD datapath rather than a shortcut. ADD on the actual chip is `ADD r`: the accumulator plus the register the instruction names plus the carry flag, result back in the accumulator, carry updated — and every part of that sentence is a piece of hardware here. The register file is addressed by the instruction's low nibble, which is exactly what OPA means in the real encoding. A 4-bit ripple adder does the arithmetic. A one-bit carry register holds the flag, which ADD both reads and writes, so a chain of ADDs carries across nibbles the way the real machine's multi-digit arithmetic does. The new idea is the source mux: LDM and ADD both write the accumulator, so accLoad says whether to write and accFromAlu says from where. That is the first control line here that steers a datapath rather than simply enabling something, and it is the shape all datapath control eventually takes.",
      "hints": {
        "CLK": "the clock — three edges per instruction",
        "RST": "back to FETCH at address 0",
        "P": "the phase ring: FETCH, DECODE, EXEC",
        "PC": "program counter",
        "IR": "instruction register",
        "ACC": "the accumulator",
        "R": "the register the instruction is naming",
        "CY": "the carry flag"
      },
      "state": {
        "title": "Phases and control lines",
        "columns": 4,
        "key": "amber = the active phase · blue = a control line firing"
      }
    },
    {
      "id": "jcnmachine",
      "group": "CMOS · Machines",
      "name": "Conditional Machine",
      "desc": "The last bring-up milestone, and the one that makes the machine loop on its own condition rather than run in a straight line. The program counts down, and the way it does so is worth a second look: r0 holds 15, and adding 15 is how you subtract 1. The accumulator is four bits, so arithmetic is mod 16 — 4 + 15 = 19, which is 16 + 3, and the 16 does not fit so it falls out as the carry, leaving 3. Every pass takes one off. That is two's complement: in four bits the top bit carries weight −8, so 1111 is −8+4+2+1 = −1. It is also why the 4004 has no subtract-immediate instruction, since adding the complement uses hardware that already exists. The JCN sends control back to the ADD while the result is not yet zero, so the loop runs three times and then falls through — you can watch both branches of the same instruction in one pass. JCN's operand is a condition mask, not a code, and that is the character of the instruction: bit 2 tests the accumulator for zero, bit 1 tests the carry, bit 0 tests the TEST pin, and bit 3 inverts the whole result. So JCN 4 jumps when the accumulator is zero and JCN 12 jumps when it is not — one OR over the same three inputs, with one bit deciding whether to take the answer or its complement. One instruction covers what a conventional machine spends four opcodes on. The loop body sits at address 4 because it has to. This machine has no two-byte fetch yet, so a jump's target is the low bits of its own byte — and for JCN that nibble is also the condition mask, which welds the two together: mask 12 is 1100, whose low three bits are 100, so “jump if not zero” and “jump to 4” are the same instruction. On the real chip they live in different bytes and no such coupling exists. It is the most visible consequence of the missing two-byte fetch, left in the open rather than hidden behind a program arranged to avoid it. TEST was a real pin on the chip, an external input a program could branch on, which is how a 4004 polled a keyboard without interrupts; it is a switch here because that is what it was.",
      "hints": {
        "CLK": "the clock — three edges per instruction",
        "RST": "back to FETCH at address 0",
        "TEST": "the TEST pin — an external input JCN can branch on",
        "P": "the phase ring: FETCH, DECODE, EXEC",
        "PC": "program counter",
        "IR": "instruction register — for JCN its low nibble is the condition mask",
        "ACC": "the accumulator",
        "Z": "the accumulator is zero (computed, not stored)",
        "CY": "the carry flag",
        "TK": "the condition tree says this jump takes"
      },
      "state": {
        "title": "Phases and control lines",
        "columns": 4,
        "key": "amber = the active phase · blue = a control line firing"
      }
    },
    {
      "id": "twobyte",
      "group": "CMOS · Machines",
      "name": "Two-Byte Machine",
      "desc": "A jump's destination now comes from a byte of its own, and that one change makes the machine feel like a computer.\n\nEvery earlier machine here took a jump target from the low bits of the jump instruction itself, because there was nowhere else to get one. That welded two unrelated things together: JCN's low nibble is its condition mask, so \"jump if not zero\" and \"jump to address 4\" had to be the same four bits. You could have one or the other, not both.\n\nA real 4004 instruction that needs an address carries a second byte. Honouring that costs a phase — FETCH the opcode, DECODE it, FETCH2 the operand, EXEC — and buys the machine a genuine instruction set. Watch the operand register: it stays empty through one-byte instructions and fills during FETCH2 when there is a second byte to read.\n\nThe part that is easy to miss is the program counter advancing twice, once for the opcode and once to step over the operand. Without that second step the operand byte is simply the next thing fetched, and the machine executes an address as though it were an instruction — which is exactly what the earlier machines do, visible in their listings as a NOP that is really an operand.\n\nThe program runs an ISZ loop, which is the other thing a real second byte buys. ISZ increments a register and jumps while the result is NOT zero — the name describes the fall-through, not the branch. Starting from 13 it counts 14, 15, then falls through when the register wraps to 0.\n\nThe increment has a block of its own, because the chip does. Sheet 1 of Intel's schematic is titled \"ADDRESS REGISTER, INCREMENTER AND INDEX\" and draws the incrementer separately from the adder: adding one needs only a half-adder chain, four XORs and three ANDs, rather than four full adders.\n\nWatch the hold register beside it. The register file's cells are transparent while being written, so wiring the incrementer straight to the read bus closes a loop — read, increment, write, read again — that free-runs while the write is open and lands on an arbitrary value. The register advances by an irregular amount each pass, which reads like a decode fault and is a topology one. Capturing the value first, on the opposite clock phase, is what makes it increment exactly once.\n\nThese four phases are not the 4004's. The real chip runs eight (A1 A2 A3 M1 M2 X1 X2 X3), five of which exist only to push a 12-bit address and pull an 8-bit instruction through a four-bit bus one nibble at a time, because it had sixteen pins. That is packaging rather than architecture, and this project drops it deliberately. What survives is the part the instruction set genuinely requires: an operand has to be read before it can be used.",
      "hints": {
        "CLK": "the clock — four edges per instruction now",
        "RST": "back to FETCH at address 0",
        "TEST": "the TEST pin — an external input JCN can branch on",
        "P": "the phase ring: FETCH, DECODE, FETCH2, EXEC",
        "PC": "program counter",
        "IR": "instruction register — the opcode byte",
        "OPR": "operand register — the second byte, and the jump target",
        "ACC": "the accumulator",
        "Z": "the accumulator is zero",
        "TK": "this jump takes"
      },
      "state": {
        "title": "Phases and control lines",
        "columns": 4,
        "key": "amber = the active phase · blue = a control line firing"
      }
    },
    {
      "id": "submachine",
      "group": "CMOS · Machines",
      "name": "Subtract and Exchange",
      "desc": "SUB, LD and a real XCH — three instructions that all need one thing no earlier machine had: a path from the register file back into the accumulator.\n\nSUB is the cheap one, and it is cheap for a reason worth seeing. The adder is already there, and subtracting is adding the complement, so SUB and ADD are the same hardware with one XOR per bit deciding whether the operand is inverted. Nothing else is added.\n\nThe carry convention is where the 4004 gets strange, and it is worth reading twice. On the way in, carry=1 means a borrow happened. On the way out, carry=1 means a borrow did NOT happen. The sense flips across the instruction — Intel's own manual tells you to put a CMC between successive SUBs when chaining them across nibbles. A machine that used the carry directly would still pass every single-digit test and fail only on multi-digit subtraction. The same XOR that inverts the operand also inverts the carry, so the odd convention costs exactly nothing in gates.\n\nXCH is the one worth stepping through. On the 4004 it is the only way to write a register at all — the chip has no store instruction — so it has to be a genuine exchange, both directions in one instruction. The obvious implementation does not work: the register file's cells are transparent while being written, so writing a row while reading the same row means the read port shows the value going in, and the swap collapses into a one-way copy.\n\nThe fix here is the one the real chip uses. A two-phase clock splits the execute phase in two: the accumulator samples the register file while the clock is high, and the register file's write lands while it is low. Read first, write second, no scratch register anywhere. Intel's schematic shows the same separation done with the chip's φ1/φ2 pins, and an ADB register on the adder's second input doing the holding — which already existed to feed the adder for ADD.\n\nThe program computes 9 − 4 = 5 and parks it in a register. Watch the carry after SUB: it is set, which on this machine means the subtraction did not borrow.",
      "hints": {
        "CLK": "the clock — four phases per instruction",
        "RST": "back to FETCH at address 0",
        "P": "the phase ring: FETCH, DECODE, FETCH2, EXEC",
        "PC": "program counter",
        "IR": "instruction register",
        "ACC": "the accumulator",
        "R": "the register file's read bus",
        "CY": "carry — after SUB, 1 means no borrow",
        "Z": "the accumulator is zero"
      },
      "state": {
        "title": "Registers",
        "columns": 4,
        "key": "amber = the register this instruction addresses"
      }
    },
    {
      "id": "accgroup",
      "group": "CMOS · Machines",
      "name": "Accumulator Group",
      "desc": "Thirteen instructions sharing a single opcode, and a lesson in how an instruction set grows faster than the silicon under it.\n\nThe 4004 has sixteen opcodes, which is not many. Two of them are escapes: opcode 1111 puts a second opcode in its low nibble, and that is where thirteen accumulator instructions live. This machine decodes that second nibble with the same 4-to-16 decoder used for ROM row select, register file addressing and the first-level instruction decode — the same block, wearing a fourth hat.\n\nWatch the source mux. Every machine before this one could write the accumulator from two places. This one has eight, and most of them need no arithmetic at all.\n\nIAC and DAC are the adder fed a constant. Increment adds 1; decrement adds 15, because at four bits 15 is −1, so there is no borrow network anywhere in this machine. CMA is four inverters. RAL and RAR are wires — a rotate left reads each bit from the one below it, right from the one above — with the carry spliced in to close the ring, which is what makes the pair reversible and how a four-bit machine shifts values wider than four bits. CLB is nothing at all: select no source and the mux drives zero.\n\nFour of them are here because this was a calculator chip. DAA is decimal adjust: after a binary add, a BCD digit can land in the illegal 10–15 range or can have carried, and adding 6 fixes both. Its carry rule is the subtle one — Intel's manual underlines that DAA may set the carry but never resets it, so a digit that adjusts without carrying out leaves an existing carry alone. Implemented as an ordinary add it would pass every single-digit test and fail only on multi-digit BCD.\n\nKBP is the keyboard scanner's helper: a one-of-n code from a row of key contacts becomes the binary position of the key. Two keys at once gives 1111 rather than a wrong answer — a real thing on a real keyboard, reported distinctly instead of silently resolved, which is what separates it from the priority encoder you would reach for by instinct.\n\nTCC moves the carry into the accumulator and clears it; TCS writes 9 or 10 depending on the carry and then clears it too. Both exist for multi-digit decimal arithmetic, and both are pure wiring.\n\nThe program walks a BCD adjustment, a keyboard decode and a rotate, then falls off the end of the eight-byte ROM and wraps. There is no jump instruction in it — the program counter wrapping is the loop.",
      "hints": {
        "CLK": "the clock — four edges per instruction",
        "RST": "back to FETCH at address 0",
        "P": "the phase ring: FETCH, DECODE, FETCH2, EXEC",
        "PC": "program counter",
        "IR": "instruction register — the opcode byte",
        "ACC": "the accumulator",
        "CY": "the carry — its own one-bit register with four instructions",
        "Z": "the accumulator is zero"
      },
      "state": {
        "title": "Phases and control lines",
        "columns": 4,
        "key": "amber = the active phase · blue = a control line firing"
      }
    },
    {
      "id": "nmosring",
      "group": "NMOS · Basics",
      "name": "NMOS Ring Oscillator",
      "desc": "The same trick as the relay Ring Oscillator — an odd number of inversions in a loop can never settle — but in silicon. Two inverters plus the run/stop NAND makes three, so raising RUN closes the ring and it never stops. Watch the asymmetry the relay version cannot show you: each stage snaps down hard through its transistor and drifts up slowly through its load resistor, so the waveform is lopsided. That is the NMOS pull-up being weak, and it is half of why CMOS won."
    },
    {
      "id": "cmosring",
      "group": "CMOS · Basics",
      "name": "CMOS Ring Oscillator",
      "desc": "The same ring in complementary CMOS. Both edges are now driven hard — the P-channel pulls up as strongly as the N-channel pulls down — so the waveform is symmetric where the NMOS one drifts. Ring oscillators are how you actually measure a fabrication process: build one, count its frequency, and you know how fast the transistors came out."
    },
    {
      "id": "nmosxor",
      "group": "NMOS · Gates",
      "name": "NMOS XOR",
      "desc": "Four NAND gates: 8 transistors and 4 load resistors. Set it beside the CMOS XOR (16 transistors) and the relay XOR (two changeover contacts) and you have the clearest device-count comparison in the library. XOR is the function that punishes transistor logic — every technology pays for it, and the relay pays least."
    },
    {
      "id": "nmosdec",
      "group": "NMOS · Gates",
      "name": "NMOS 2-to-4 Decoder",
      "desc": "Two address lines select exactly one of four outputs — the same job the relay contact tree does, and the same operation a memory row decoder performs. Every AND here is a NAND plus an inverter, so a decoder is one of the places the non-inverting tax gets paid four times over."
    },
    {
      "id": "nmoslatch",
      "group": "NMOS · Memory",
      "name": "NMOS D Latch",
      "desc": "Cross-coupled NORs with steering, not the transmission-gate latch CMOS uses — a pass gate needs a complementary pair, and NMOS has no P-channel. Watch the strengths: a held 1 is only weakly driven by the load resistor while a held 0 is pulled hard to ground. The CMOS latch holds both hard."
    },
    {
      "id": "nmosadd4",
      "group": "NMOS · Arithmetic",
      "name": "NMOS 4-bit Adder",
      "desc": "Four full adders chained carry to carry — the same topology as the relay ripple adder, built from NMOS gates. 100 transistors and 56 resistors. Set A=1111 and flip B0 to watch the carry ripple the whole length of the chain, exactly as the relay version does but far faster."
    },
    {
      "id": "nmosalu",
      "group": "NMOS · Arithmetic",
      "name": "NMOS 4-bit ALU",
      "desc": "Four functions instead of the CMOS ALU’s six, and the difference is the whole lesson. The CMOS version steers its result onto a shared bus with transmission gates — which need a complementary pair. NMOS has no P-channel, so no pass gate, no tri-state, and no shared bus: every function must be AND-ed with its select line and the results OR-ed back together. That mux tree is what a machine without tri-state costs, and it is precisely what the Tri-State Bus circuit exists to explain.",
      "hints": {
        "A": "first operand",
        "B": "second operand",
        "F": "function select — see the table below",
        "Y": "result",
        "Cout": "carry out (add only)"
      },
      "table": {
        "title": "Function select (F)",
        "rows": [
          {
            "code": "00",
            "name": "ADD",
            "note": "A + B"
          },
          {
            "code": "01",
            "name": "AND",
            "note": "bitwise"
          },
          {
            "code": "10",
            "name": "OR",
            "note": "bitwise"
          },
          {
            "code": "11",
            "name": "XOR",
            "note": "bitwise"
          }
        ]
      }
    },
    {
      "id": "nmosnand",
      "group": "NMOS · Gates",
      "name": "NMOS NAND",
      "desc": "Two N-channels in series under a resistor load. Compare it with the CMOS NAND: same pull-down network, but no complementary pull-up above it — half the transistors, and the output is only ever pulled high weakly. Whenever the output is low there is a permanent path from the rail to ground through the load, which is the static current that made NMOS chips run hot."
    },
    {
      "id": "nmosnor",
      "group": "NMOS · Gates",
      "name": "NMOS NOR",
      "desc": "Two N-channels in parallel: either input high pulls the output down. In NMOS the NOR is the cheap gate — parallel transistors do not stack their resistance the way a series chain does, so NOR was the workhorse where CMOS designers prefer NAND."
    },
    {
      "id": "nmosand",
      "group": "NMOS · Gates",
      "name": "NMOS AND",
      "desc": "A NAND followed by an inverter, and you can see the cost: AND needs a whole second stage with its own load resistor, so it burns current in two places instead of one. This is why the primitive gate of any technology is the inverting one, and why designers rewrite logic to avoid the non-inverted forms."
    },
    {
      "id": "nmosor",
      "group": "NMOS · Gates",
      "name": "NMOS OR",
      "desc": "A NOR plus an inverter. Same story as AND: the extra stage doubles the static current and adds a full gate delay, for an output that a NOR feeding the next stage could often have given you for free."
    },
    {
      "id": "rom8",
      "group": "NMOS · Arrays",
      "name": "Program ROM",
      "desc": "A real NMOS mask ROM: 8 words of 8 bits. A transistor at a site pulls its bit line down, so it stores a 0 — and a site storing 1 is literally empty silicon, which you can see on the canvas. Resistors pull every line up, so the array is a wired-AND. A bare switch matrix would sneak-path here (the selected row backfeeds through an unselected one and lights bits that are not stored); isolated gates make that structurally impossible, which is why ROM is built this way and why diode matrices existed before it.",
      "hints": {
        "A": "address — which of the 8 stored words to read",
        "D": "the word at that address (bit = 1 where the array has no transistor)"
      },
      "state": {
        "title": "Stored contents",
        "columns": 4,
        "key": "amber = the word the address lines are selecting"
      }
    },
    {
      "id": "romcmos",
      "group": "CMOS · Memory",
      "name": "CMOS Program ROM",
      "desc": "The same 8 × 8 array with no resistors anywhere — a P-channel per bit line instead. It reads in two phases: with PRE low the pull-ups charge every line high, then PRE goes high, the pull-ups switch off, and the selected row discharges only the lines that have a transistor. Nothing ever fights, so unlike the NMOS version there is no path from rail to ground and no static current — which is the whole reason CMOS replaced NMOS. The cost is that a line reading 1 is only floating on its own charge (dashed, Z), so the reading is valid until the charge leaks away. That is dynamic logic, and it is why such chips have a minimum clock speed as well as a maximum.",
      "hints": {
        "A": "address — which of the 8 stored words to read",
        "PRE": "low charges every bit line; raise it to evaluate the word",
        "D": "the word at that address"
      },
      "table": {
        "title": "Read cycle",
        "rows": [
          {
            "code": "1.",
            "name": "PRE low",
            "note": "pull-ups on, every line charged high"
          },
          {
            "code": "2.",
            "name": "PRE high",
            "note": "pull-ups off, the row discharges its 0s"
          }
        ]
      },
      "state": {
        "title": "Stored contents",
        "columns": 4,
        "key": "amber = the word the address lines are selecting"
      }
    },
    {
      "id": "alu4",
      "group": "CMOS · Arithmetic",
      "name": "4-bit ALU",
      "desc": "Six functions over two nibbles, chosen by F. Every function is computed at once and a transmission gate steers the selected one onto a shared result bus — one wire with six possible drivers, the way a CPU does it, not a mux tree per destination. 538 transistors, composed from gate modules rather than placed by hand. Codes 6 and 7 select nothing, and the bus floats.",
      "hints": {
        "A": "first operand",
        "B": "second operand",
        "F": "function select — see the table below",
        "Y": "result",
        "Cout": "carry out of the top bit (borrow flag when subtracting)"
      },
      "table": {
        "title": "Function select (F)",
        "rows": [
          {
            "code": "000",
            "name": "ADD",
            "note": "A + B"
          },
          {
            "code": "001",
            "name": "SUB",
            "note": "A − B, two’s complement"
          },
          {
            "code": "010",
            "name": "AND",
            "note": "bitwise"
          },
          {
            "code": "011",
            "name": "OR",
            "note": "bitwise"
          },
          {
            "code": "100",
            "name": "XOR",
            "note": "bitwise"
          },
          {
            "code": "101",
            "name": "SHL",
            "note": "A shifted left one bit"
          },
          {
            "code": "11x",
            "name": "—",
            "note": "nothing selected; the bus floats"
          }
        ]
      },
      "state": {
        "title": "All six results",
        "columns": 3,
        "key": "read off the circuit, not recomputed. The logic gates all compute at once; ADD and SUB share one adder, so only the selected one has a value (· = not being computed)."
      }
    },
    {
      "id": "halfadd",
      "group": "Relays · Arithmetic",
      "name": "Half Adder",
      "desc": "XOR gives the sum bit, a series pair gives the carry: 1+1=10. Five contacts on two relays."
    },
    {
      "id": "fulladd",
      "group": "Relays · Arithmetic",
      "name": "Full Adder",
      "desc": "Three relays add three bits: a changeover staircase computes the parity (sum) and three series pairs vote on the majority (carry). The building block of every adder."
    },
    {
      "id": "add4",
      "group": "Relays · Arithmetic",
      "name": "4-bit Ripple Adder",
      "desc": "Four full adders chained carry-to-carry. Set A=1111 then flip B0 on and watch the carry ripple down the whole row — this is why adders have a \"critical path\"."
    },
    {
      "id": "add8",
      "group": "Relays · Arithmetic",
      "name": "8-bit Ripple Adder",
      "desc": "24 relays, 88 contacts, numbers up to 255. Zoom out to see the machine, zoom in to watch any single contact. Try 11111111 + 1."
    },
    {
      "id": "addsub4",
      "group": "Relays · Arithmetic",
      "name": "4-bit Adder / Subtractor",
      "desc": "Flip SUB and a relay bank inverts every B bit while the same signal injects a carry-in: two’s complement, A−B = A+(~B)+1, in hardware. The seed of an ALU."
    }
  ]
}
